<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xxxx.whiteboard.mapper.ColorMapper">

    <select id="getColorId">
        SELECT `color_id` from `color`
        WHERE color.r = `${r}` AND color.g = `${g}` AND color.b = `${b}` AND color.a = `${a}`
    </select>

</mapper>