<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xxxx.whiteboard.mapper.RoomMapper">


    <!-- 房间里人数自动递增1 -->
    <update id="personNumIncr">
        UPDATE room
        SET people_num = people_num + 1
        WHERE room_id = ${roomId}
    </update>

    <!-- 房间里人数自动递减1 -->
    <update id="personNumDecr">
        UPDATE room
        SET people_num = people_num - 1
        WHERE room_id = ${roomId}
    </update>


    <!-- 在room中将总页数 ++ -->
    <update id="roomPageIncr">
        UPDATE room
        SET page_count = page_count + 1
        WHERE room_id = ${roomId}
    </update>

    <!-- 在room中将总页数自减 -->
    <update id="roomPageDecr">
        UPDATE room
        SET page_count = page_count - 1
        WHERE room_id = ${roomId}
    </update>



</mapper>